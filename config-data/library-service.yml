server:
  port: 8093
  servlet:
    context-path: /library
    session:
      timeout: 21600s
eureka: 
  client:
    serviceUrl: 
      defaultZone: http://localhost:8099/eureka/
  instance:
    prefer-ip-address: true
    hostname: localhost